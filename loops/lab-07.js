var names = ['Fred', 'Wilma', 'Barney', 'Betty'];
var eNames = [];
var i = 0

var eNames = names.indexOf("e");

while (i < names.length) {
    i++;
  }
  console.log(eNames);


/*
Write a for loop to loop over the array and use the loop to check each name to see if it contains the letter 'e'. If it does, add it to the eNames array.
Note: you might want to remind yourself of how the `indexOf` function works on string. console.log(eNames);
Expected Output:
[ 'Fred', 'Barney', 'Betty' ]
*/
